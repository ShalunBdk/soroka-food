# 🚀 Быстрый старт Soroka Food

## Первый запуск (пошагово)

### Шаг 1: Запустите PostgreSQL

```bash
net start postgresql-x64-17
```

### Шаг 2: Создайте базу данных

```bash
"C:\Program Files\PostgreSQL\17\bin\createdb" -U postgres soroka-food
```

Если попросит пароль, введите пароль от PostgreSQL.

### Шаг 3: Настройте .env файл

Откройте `soroka-food-backend\.env` и убедитесь, что пароль правильный:

```env
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ_ОТ_POSTGRES@localhost:5432/soroka-food"
```

⚠️ **Важно:** Специальные символы нужно кодировать:
- `#` → `%23`
- `@` → `%40`

### Шаг 4: Запустите миграции и seed

```bash
npm run prisma:migrate
npm run prisma:seed
```

### Шаг 5: Запустите приложение

```bash
npm run dev
```

Готово! 🎉

- Frontend: http://localhost:5173
- Backend: http://localhost:3000

### Шаг 6: Войдите в админ-панель

Перейдите на http://localhost:5173/admin/login

Credentials:
- Username: `admin`
- Password: `admin123`

---

## Ежедневное использование

После первой настройки, для запуска просто:

```bash
npm run dev
```

Эта команда запустит и frontend, и backend одновременно!

---

## Остановка приложения

Нажмите `Ctrl + C` в терминале

---

## Полезные команды

```bash
# Открыть Prisma Studio (просмотр БД)
npm run prisma:studio

# Пересоздать данные в БД
npm run prisma:seed

# Собрать production версию
npm run build

# Запустить production версию
npm start
```

---

## Проблемы?

### PostgreSQL не запускается
```bash
# Проверьте статус
sc query postgresql-x64-17
```

### База данных уже существует
Это нормально! Просто пропустите создание БД и переходите к миграциям.

### Порт 3000 или 5173 занят
Другое приложение использует эти порты. Закройте его или измените порт в настройках.

### Ошибка миграции "Advisory Lock Timeout"
1. Закройте все подключения к БД
2. Перезапустите PostgreSQL:
   ```bash
   net stop postgresql-x64-17
   net start postgresql-x64-17
   ```
3. Попробуйте снова

---

**Нужна помощь?** Смотрите `README.md` для полной документации.
