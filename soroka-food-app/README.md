# Soroka Food - Кулинарный Блог

Современное веб-приложение для кулинарного блога, построенное на React + TypeScript + Vite.

## Технологический стек

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript для надежности кода
- **Vite** - быстрый инструмент сборки
- **React Router** - навигация между страницами

## Структура проекта

```
soroka-food-app/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Header/         # Шапка сайта с навигацией
│   │   ├── Footer/         # Подвал сайта
│   │   ├── Breadcrumbs/    # Навигационная цепочка
│   │   ├── RecipeCard/     # Карточка рецепта
│   │   ├── Sidebar/        # Боковая панель
│   │   ├── Pagination/     # Пагинация
│   │   ├── Newsletter/     # Форма подписки
│   │   └── SocialLinks/    # Социальные сети
│   ├── pages/              # Страницы приложения
│   │   ├── Home.tsx        # Главная страница со списком рецептов
│   │   └── RecipeDetail.tsx # Детальная страница рецепта
│   ├── types/              # TypeScript типы и интерфейсы
│   │   └── index.ts
│   ├── data/               # Моковые данные (временно)
│   │   └── recipes.ts
│   ├── styles/             # Стили страниц
│   ├── App.tsx             # Главный компонент с роутингом
│   └── main.tsx            # Точка входа приложения
```

## Установка и запуск

### Предварительные требования

- Node.js 16+ и npm

### Установка зависимостей

```bash
cd soroka-food-app
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется по адресу: http://localhost:5173/

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## Функционал

### Реализовано

- ✅ Главная страница со списком рецептов в виде карточек
- ✅ Детальная страница рецепта с ингредиентами и инструкциями
- ✅ Навигация между страницами через React Router
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Боковые панели с категориями и популярными рецептами
- ✅ Пагинация для списка рецептов
- ✅ Форма подписки на рассылку
- ✅ Социальные ссылки
- ✅ Форма комментариев с рейтингом
- ✅ Вкладки для переключения между информацией о рецепте
- ✅ TypeScript типизация всех компонентов

### Планируется для интеграции с backend

- 🔲 API для получения рецептов
- 🔲 Система авторизации пользователей
- 🔲 Личный кабинет пользователя
- 🔲 Возможность добавления рецептов
- 🔲 Реальные комментарии и рейтинги
- 🔲 Поиск по рецептам
- 🔲 Фильтрация по категориям
- 🔲 Админ-панель для управления контентом
- 🔲 Загрузка изображений
- 🔲 Избранные рецепты
- 🔲 Подписка на рассылку (реальная интеграция)

## Ключевые компоненты

### Header
Шапка сайта с логотипом, навигационным меню и поиском.

### RecipeCard
Переиспользуемая карточка рецепта с изображением, названием, описанием и метаданными.

### Home
Главная страница с сеткой рецептов, фильтрами и пагинацией.

### RecipeDetail
Детальная страница рецепта с полной информацией, ингредиентами, инструкциями и комментариями.

## Типы данных

Все типы данных определены в `src/types/index.ts`:

- **Recipe** - основная информация о рецепте
- **RecipeDetail** - расширенная информация с ингредиентами и инструкциями
- **Ingredient** - ингредиент рецепта
- **InstructionStep** - шаг приготовления
- **Nutrition** - пищевая ценность
- **Comment** - комментарий пользователя
- **Category** - категория рецептов
- **SidebarSection** - секция боковой панели

## Подготовка к интеграции с Backend

Текущая версия использует моковые данные из `src/data/recipes.ts`.

Для интеграции с backend нужно:

1. Создать API сервис в `src/services/api.ts`
2. Заменить импорты данных на API вызовы
3. Добавить обработку состояний загрузки и ошибок
4. Реализовать отправку форм (комментарии, подписка)

Пример структуры API сервиса:

```typescript
// src/services/api.ts
export const api = {
  getRecipes: async () => {...},
  getRecipeById: async (id: number) => {...},
  createComment: async (recipeId: number, comment: Comment) => {...},
  subscribe: async (email: string) => {...}
};
```

## Стилизация

Проект использует обычный CSS без препроцессоров. Стили организованы по компонентам:
- Каждый компонент имеет свой CSS файл
- Глобальные стили в `App.css`
- Стили страниц в `src/styles/`

## Следующие шаги

1. **Backend интеграция**: Создать REST API или GraphQL endpoint
2. **Авторизация**: Реализовать систему регистрации/входа
3. **База данных**: Настроить БД для хранения рецептов, пользователей и комментариев
4. **Админ-панель**: Создать интерфейс для управления контентом
5. **Облачное хранилище**: Интегрировать для загрузки изображений
6. **SEO оптимизация**: Добавить мета-теги и SSR при необходимости

## Лицензия

MIT
