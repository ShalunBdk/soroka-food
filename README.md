# 🍳 Soroka Food - Профессиональный Кулинарный Блог

<div align="center">

**Готовое к production fullstack-решение для кулинарного блога**

[![React 19](https://img.shields.io/badge/React-19-61dafb?logo=react)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-3178c6?logo=typescript)](https://www.typescriptlang.org/)
[![Node.js](https://img.shields.io/badge/Node.js-18+-339933?logo=node.js)](https://nodejs.org/)
[![Express 5](https://img.shields.io/badge/Express-5-000000?logo=express)](https://expressjs.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-12+-336791?logo=postgresql)](https://www.postgresql.org/)
[![Prisma](https://img.shields.io/badge/Prisma-ORM-2D3748?logo=prisma)](https://www.prisma.io/)

[Демо](https://soroka-demo.shbdk.ru/) • [Возможности](#-ключевые-возможности) • [Установка](#-docker-production-build) • [Документация](#-документация) • [Стек](#-технологический-стек)

</div>

<a href="https://soroka-demo.shbdk.ru"><img src="https://raw.githubusercontent.com/shalunbdk/soroka-food/main/soroka-food-app/public/soroka-food.png" alt="soroka-demo.shbdk.ru" /></a>
<a href="https://soroka-demo.shbdk.ru/admin"><img src="https://raw.githubusercontent.com/shalunbdk/soroka-food/main/soroka-food-app/public/soroka-food-admin.png" alt="soroka-demo.shbdk.ru/admin" /></a>

---

## ✨ Ключевые возможности

### 👤 Для пользователей

#### 📖 Богатый контент
- **Детальные рецепты** с пошаговыми инструкциями и фото для каждого шага
- **Умный расчёт ингредиентов**:
  - Группировка по категориям (например, "Для соуса", "Для теста")
  - 10 единиц измерения (г, кг, мл, л, шт, ст.л., ч.л., стакан, щепотка, по вкусу)
  - Автоматический пересчёт при изменении количества порций (+/- кнопки)
- **Время приготовления**:
  - Раздельные поля для подготовки и приготовления
  - Умное форматирование (90 мин → 1 ч 30 мин)
  - Отображение общего времени с разбивкой
- **Пищевая ценность** - калории (на 100г), белки, жиры, углеводы
- **Умные фильтры** - по категориям, теги
- **Продвинутый поиск** - по названию, описанию, тегам (регистронезависимый, с пагинацией)
- **Сортировка** - новинки, популярные, с фото
- **Лучшие рецепты** - топ по просмотрам

#### 💬 Интерактивность
- **Комментарии с рейтингами** (1-5 звёзд) + автоматическая модерация + pagination (20 per page, "Load More" button)
- **Социальный шаринг** - VK, Telegram, WhatsApp, копирование ссылки
- **QR-коды для быстрого доступа** - в разделе "Поделиться" (скрыты на мобильных ≤768px)
- **Умный счётчик просмотров** - throttling 24 часа через localStorage
- **Подписка на рассылку** - сбор email для newsletter

#### 🎯 Удобство
- **Адаптивный дизайн** - работает на всех устройствах
- **Печать рецептов** 🖨️:
  - Оптимизированная верстка для А4 (компактные отступы, читаемые шрифты 10-11pt)
  - Автоматическое масштабирование ингредиентов при изменении порций
  - Изображения шагов приготовления (max 60×50mm каждое)
  - QR-код для быстрого открытия на телефоне (100px, правый нижний угол)
  - Технология React Portal для чистой печати без лишнего контента
- **Быстрая загрузка** - автоматическая конвертация в WebP (main 1200px + thumbnail 300px, 25-35% меньше JPEG), HTTP кеширование (1 год для uploads)
- **Динамическая навигация** - автоматическое обновление категорий и статистики
- **Статические страницы** - О нас, Контакты, Правила, Реклама (редактируемые через админку)

---

### 🛡️ Для администраторов

#### 🎛️ Мощная админ-панель

**Dashboard**
- Статистика в реальном времени - рецепты, пользователи, комментарии, подписчики
- Быстрый доступ к последним рецептам и комментариям

**Управление рецептами**
- WYSIWYG редактор (react-quill-new) - визуальное редактирование с форматированием
- **Умное управление ингредиентами**:
  - Группировка с опциональными категориями (например, "Для соуса", "Для теста")
  - Выбор единиц измерения из 10 вариантов (г, кг, мл, л, шт, ст.л., ч.л., стакан, щепотка, по вкусу)
  - Поле количества с поддержкой десятичных дробей (запятая или точка)
  - Автоматическая генерация строки "количество + единица"
- **Управление временем**:
  - Опциональное поле "Время подготовки"
  - Обязательное поле "Время приготовления"
  - Автоматическое форматирование в часы/минуты (90 мин → 1 ч 30 мин)
- Загрузка изображений с превью и валидацией (JPEG/PNG/WebP, max 5MB)
- Автоматическая конвертация в WebP: main 1200px + thumbnail 300px
- Мультизагрузка - фото для каждого шага инструкции (max 5)
- Автогенерация slug - транслитерация русского в латиницу
- Статусы - DRAFT (черновик) / PUBLISHED (опубликован)
- Теги - автозагрузка существующих + создание на лету

**Управление категориями и тегами**
- Категории - создание/редактирование/удаление с авто-slug
- Теги - просмотр статистики использования, переименование глобально, удаление с подтверждением

**Модерация комментариев**
- Массовые действия - выбор нескольких комментариев для обработки
- Bulk операции - одобрить/пометить как спам/вернуть в ожидание/удалить
- Фильтрация по статусу (все/в ожидании/одобрены/спам)
- Навигация с прямыми ссылками на рецепты

**Настройки сайта (ADMIN+)**
- Брендинг - название сайта, логотип, favicon
- SEO - мета-описание, ключевые слова
- Социальные сети - ссылки на профили
- Контакты - email, телефон, адрес
- Динамическое применение - изменения видны сразу

**Email/Newsletter система (ADMIN+)**
- SMTP конфигурация (AES-256 шифрование паролей)
- Автоматические newsletters при публикации рецептов
- Double opt-in защита от email bombing
- Customizable Handlebars шаблоны
- Batch отправка (50/batch) с retry логикой
- История отправки с фильтрацией

**Спам-фильтр (SUPER_ADMIN)**
- Toggle фильтры - включить/выключить каждый фильтр
- Регулировка порогов - слайдеры для maxUrls (0-10) и capsPercentage (50-100%)
- Кастомные ключевые слова - добавление/удаление спам-слов
- Умный UI - слайдеры сохраняются только при отпускании

**Логи администраторов (SUPER_ADMIN)**
- Полный аудит всех действий админов и модераторов
- Детальная информация - пользователь, действие, ресурс, IP, user agent, timestamp
- Фильтрация по пользователю, типу действия, ресурсу, датам
- Пагинация настраиваемая (10/25/50/100 записей)
- Expandable детали - клик для просмотра JSON (old/new значения)
- Append-only - логи нельзя удалить или изменить

#### 👥 Управление пользователями (ADMIN+)

**3-уровневая иерархия ролей**
| Роль | Описание | Доступ |
|------|----------|--------|
| **SUPER_ADMIN** | Главный администратор | Всё + спам-фильтры + логи + SMTP |
| **ADMIN** | Администратор | Контент + настройки + newsletter + создание модераторов |
| **MODERATOR** | Модератор | Рецепты + категории + теги + комментарии |

**Матрица прав доступа**
| Функция | SUPER_ADMIN | ADMIN | MODERATOR |
|---------|:-----------:|:-----:|:---------:|
| Рецепты (CRUD) | ✅ | ✅ | ✅ |
| Категории, Теги | ✅ | ✅ | ✅ |
| Модерация комментариев | ✅ | ✅ | ✅ |
| Newsletter | ✅ | ✅ | ❌ |
| Статические страницы | ✅ | ✅ | ❌ |
| Настройки сайта | ✅ | ✅ | ❌ |
| Управление пользователями | ✅ | ✅ (только модераторы) | ❌ |
| Спам-фильтры | ✅ | ❌ | ❌ |
| Логи администраторов | ✅ | ❌ | ❌ |
| SMTP настройки | ✅ | ❌ | ❌ |

**Безопасность**
- Защита от самоудаления - нельзя удалить себя
- Защита привилегий - нельзя повысить свою роль
- Защита последнего админа - нельзя удалить последнего SUPER_ADMIN
- Ограничение создания - ADMIN может создавать только MODERATOR
- Генератор паролей - встроенный генератор безопасных паролей
- Валидация паролей - мин. 8 символов (заглавные + строчные + цифры)

#### 🛡️ Безопасность (Enterprise уровень)

**6-уровневая защита от спама**
1. **Honeypot ловушка** - невидимое поле для ботов
2. **Автоматическая детекция** - 5 фильтров (ключевые слова, URL, CAPS, повторы, дубликаты)
3. **Массовая модерация** - обработка множества комментариев одним кликом
4. **Настраиваемые фильтры** (только для Супер-админа):
   - Toggle включения/выключения каждого фильтра
   - Регулировка порогов (макс. URL: 0-10, макс. CAPS: 50-100%)
   - Кастомные ключевые слова для блокировки
5. **Статусы модерации** - PENDING/APPROVED/SPAM
6. **24-часовое окно дубликатов** - защита от повторных отправок

#### Дополнительная защита
- **Rate Limiting** (IP-based):
  - Общий API: 1000 запросов/15 мин
  - Логин: 5 попыток/15 мин (защита от brute-force)
  - Загрузки: 50/час
  - Комментарии: 10/15 мин
- **Helmet** - защищённые HTTP headers, CSP
- **CORS** - настраиваемые разрешённые origin
- **Sharp валидация** - проверка формата/размера/целостности изображений (max 5000×5000px)
- **Zod валидация** - строгая типизация всех входных данных
- **DOMPurify** - санитизация HTML (защита от XSS)
- **JWT токены** - безопасная аутентификация с истечением
- **Bcrypt хеширование** - пароли (10 раундов)

#### ⚡ Производительность & Оптимизация

**Database Performance**
- **Оптимизированные индексы** для быстрых запросов:
  - Recipe: status, createdAt, views, rating (сортировка/фильтрация)
  - Comment: recipeId, status, createdAt (модерация)
  - RecipeCategory: categoryId (обратные запросы)
  - AdminLog, EmailLog: userId, status, createdAt (audit trails)
- **Prisma ORM** - type-safe запросы с автоматической оптимизацией
- **N+1 защита** - использование include для предзагрузки связей

**Redis Caching**
- **Graceful Fallback** - приложение работает без Redis (lazyConnect)
- **Smart Caching** - только GET запросы, настраиваемый TTL
- **Автоматическая инвалидация** - кеш очищается при изменениях
- **Кешируемые эндпоинты**:
  - Список рецептов - 5 минут
  - Детали рецепта - 10 минут
  - Категории - 30 минут (редко меняются)
  - Поиск и статистика - 5-10 минут
- **Триггеры инвалидации**:
  - CRUD рецептов → очистка `/api/recipes*`, `/api/categories*`, `/api/recipes/stats`
  - CRUD категорий → очистка `/api/categories*`, `/api/recipes*`
  - Модерация комментариев → очистка `/api/recipes/:recipeId`
  - Bulk операции → очистка всех затронутых рецептов

**Image Optimization (Автоматическая)**
- **WebP-Only подход** - все загруженные изображения конвертируются в WebP формат
- **2 версии на изображение**:
  - Main image (max 1200px width, качество 85, effort 4)
  - Thumbnail (300px width, качество 80, effort 4)
- **Поддержка форматов на входе** - JPEG, PNG, WebP (все конвертируются в WebP на выходе)
- **Экономия места** - ~50% меньше дискового пространства по сравнению с хранением нескольких форматов
- **Широкая поддержка** - WebP работает в 97%+ браузеров (2025), на 25-35% меньше JPEG
- **Автоочистка** - исходные файлы удаляются после конвертации
- **Backward compatibility** - API возвращает `{url, thumbnail}` (single) и `{urls[], images[]}` (multiple)

**HTTP Caching (Статические файлы)**
- **Загруженные изображения** (`/uploads`) - кеш 1 год, immutable, ETag, Last-Modified
- **Frontend активы** (production):
  - JS/CSS/fonts - кеш 1 год, immutable (content-hashed filenames)
  - HTML - кеш 1 день
  - index.html - no-cache (SPA entry point всегда свежий)
- **Выгоды** - снижение нагрузки на сервер, улучшение времени загрузки, экономия трафика

**Comments Pagination**
- **Backend** - параллельные запросы (данные + count), параметры `page`/`limit` (max 100 per page)
- **Frontend** - кнопка "Загрузить еще" с отображением оставшихся комментариев
- **Формат ответа** - `{data: Comment[], pagination: {page, limit, total, totalPages, hasMore}}`
- **Default** - 20 комментариев на страницу, подгрузка по требованию

**Другие оптимизации**
- **Compression middleware** - gzip сжатие ответов
- **Lazy loading** - загрузка компонентов по требованию
- **Smart view tracking** - throttling 24h через localStorage

---

## 🚀 Быстрый старт с Docker

### Предварительные требования
- **Docker** 20.10+ ([скачать](https://www.docker.com/get-started))
- **Docker Compose** 2.0+ (обычно идёт в комплекте с Docker Desktop)

### Развёртывание за 3 минуты

#### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/yourusername/soroka-food.git
cd soroka-food
```

#### 2️⃣ Настройка переменных окружения
Создать файл `.env` в корне проекта (скопировать из .env.example):
```bash
cp .env.example .env
```

Отредактировать `.env` и заполнить обязательные поля:
```env
# Database password для PostgreSQL
DB_PASSWORD=your_secure_password_here

# JWT secret (ОБЯЗАТЕЛЬНО сгенерируйте новый!)
# Генерация: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_generated_jwt_secret_here
```

#### 3️⃣ Запуск всех сервисов
```bash
# Собрать и запустить все контейнеры (PostgreSQL, Redis, Backend, Frontend)
docker-compose up -d

# Дождаться готовности сервисов (проверка health checks)
docker-compose ps
```

#### 4️⃣ Загрузка начальных данных
```bash
# Применить миграции БД и загрузить seed (50+ рецептов)
docker-compose exec backend npx prisma migrate deploy
docker-compose exec backend node dist/prisma/seed.js
```

🎉 **Готово!** Откройте [http://localhost](http://localhost) в браузере

---

### 📦 Что запущено?

| Сервис | Контейнер | Порт | Описание |
|--------|-----------|------|----------|
| **Frontend** | soroka-food-web | 80 | React приложение (Nginx) |
| **Backend** | soroka-food-api | 3000 | Express API + static files |
| **PostgreSQL** | soroka-food-db | 5432 | База данных |
| **Redis** | soroka-food-redis | 6379 | Кеширование (опционально) |

---

### 🔧 Полезные команды Docker

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend

# Остановка всех сервисов
docker-compose down

# Остановка + удаление volumes (БД, uploads, логи)
docker-compose down -v

# Перезапуск сервиса после изменений
docker-compose restart backend

# Пересборка после изменения кода
docker-compose up -d --build

# Вход в контейнер backend (для отладки)
docker-compose exec backend sh

# Выполнение команд в контейнере
docker-compose exec backend npm run prisma:studio
docker-compose exec backend npx prisma migrate dev
```

---

### 🛠️ Локальная разработка (без Docker)

Если хотите разрабатывать локально без Docker:

<details>
<summary>Развернуть инструкции для локальной разработки</summary>

#### Предварительные требования
- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **PostgreSQL** 12+ ([скачать](https://www.postgresql.org/download/))
- **Redis** (опционально) - ([скачать](https://redis.io/download/))

#### Установка
```bash
# 1. Установить зависимости
npm run install:all

# 2. Создать БД
createdb -U postgres soroka-food

# 3. Создать файл soroka-food-backend/.env
# (см. soroka-food-backend/.env.example для примера)

# 4. Применить миграции и seed
npm run prisma:migrate
npm run prisma:seed

# 5. Запустить dev серверы
npm run dev  # Frontend :5173 + Backend :3000
```
</details>

---

### 🔑 Учётные данные по умолчанию

После `npm run prisma:seed` доступны 3 тестовых пользователя:

| Username | Пароль | Роль | Описание |
|----------|--------|------|----------|
| **admin** | admin123 | SUPER_ADMIN | Полный доступ ко всему |
| **admin2** | admin456 | ADMIN | Управление контентом + модераторы |
| **moderator** | moderator123 | MODERATOR | Только контент |

⚠️ **ВАЖНО:** Смените эти пароли в production!

### 📦 Что входит в seed?
- ✅ **50+ рецептов** - готовый контент для демонстрации
- ✅ **10 категорий** - основные типы блюд
- ✅ **100+ комментариев** - с разными статусами модерации
- ✅ **3 администратора** - для тестирования ролей
- ✅ **Настройки сайта** - готовые к использованию

---

## 🏗️ Архитектура проекта

### Структура монорепо
```
SorokaFood/
├── soroka-food-app/                # 🎨 Frontend (React 19 + TypeScript)
│   ├── src/
│   │   ├── components/             # Переиспользуемые компоненты
│   │   │   ├── Header.tsx          # Динамический header (логотип + название из settings)
│   │   │   ├── Footer.tsx          # Footer с социальными ссылками
│   │   │   ├── RecipeCard.tsx      # Карточка рецепта
│   │   │   ├── Pagination.tsx      # Компонент пагинации
│   │   │   ├── Sidebar.tsx         # Боковая панель
│   │   │   └── SiteStats.tsx       # Статистика сайта
│   │   ├── pages/                  # Страницы приложения
│   │   │   ├── Home.tsx            # Главная (с фильтрами All/Newest/Popular/Photo)
│   │   │   ├── RecipeDetail.tsx    # Детальная страница рецепта (с honeypot)
│   │   │   ├── CategoryPage.tsx    # Рецепты категории
│   │   │   ├── SearchResults.tsx   # Результаты поиска
│   │   │   ├── BestRecipes.tsx     # Лучшие рецепты
│   │   │   └── admin/              # Админ-панель
│   │   │       ├── Dashboard.tsx           # Дашборд со статистикой
│   │   │       ├── AdminRecipes.tsx        # Список рецептов
│   │   │       ├── RecipeForm.tsx          # Форма создания/редактирования
│   │   │       ├── AdminCategories.tsx     # Управление категориями
│   │   │       ├── AdminTags.tsx           # Управление тегами
│   │   │       ├── AdminComments.tsx       # Модерация комментариев (bulk actions)
│   │   │       ├── AdminUsers.tsx          # Управление пользователями
│   │   │       ├── UserForm.tsx            # Форма пользователя
│   │   │       ├── AdminStaticPages.tsx    # Редактор статических страниц
│   │   │       ├── AdminSettings.tsx       # Настройки сайта
│   │   │       ├── AdminSpamFilter.tsx     # Настройка спам-фильтров
│   │   │       └── AdminLogs.tsx           # Логи администраторов (SUPER_ADMIN only)
│   │   ├── hooks/                  # Custom hooks
│   │   │   ├── useCategories.ts    # Загрузка категорий
│   │   │   └── useSidebarData.ts   # Данные для sidebar
│   │   ├── contexts/               # React контексты
│   │   │   ├── SettingsContext.tsx # Глобальные настройки (загружаются 1 раз)
│   │   │   └── ToastContext.tsx    # Уведомления
│   │   ├── services/               # API клиенты
│   │   │   └── api.ts              # Централизованный API client (с token management)
│   │   ├── utils/                  # Утилиты
│   │   │   ├── image.ts            # getImageUrl() helper
│   │   │   ├── sanitize.ts         # DOMPurify санитизация
│   │   │   ├── viewTracker.ts      # Умный счётчик просмотров
│   │   │   └── time.ts             # formatTime() - форматирование времени (90 мин → 1 ч 30 мин)
│   │   ├── styles/                 # Plain CSS (component-scoped)
│   │   └── types/                  # TypeScript типы (совпадают с Prisma)
│   └── package.json
│
├── soroka-food-backend/            # 🔧 Backend (Express 5 + Prisma)
│   ├── src/
│   │   ├── controllers/            # Бизнес-логика
│   │   │   ├── authController.ts           # Аутентификация (JWT)
│   │   │   ├── recipeController.ts         # CRUD рецептов
│   │   │   ├── adminController.ts          # Админ-операции
│   │   │   ├── commentController.ts        # Комментарии (с автоспам)
│   │   │   ├── categoryController.ts       # Категории
│   │   │   ├── tagController.ts            # Теги (rename/delete)
│   │   │   ├── userController.ts           # Управление пользователями
│   │   │   ├── staticPageController.ts     # Статические страницы
│   │   │   ├── spamFilterController.ts     # Настройки спам-фильтров
│   │   │   ├── settingsController.ts       # Настройки сайта
│   │   │   ├── adminLogController.ts       # Логи администраторов (SUPER_ADMIN)
│   │   │   └── uploadController.ts         # Загрузка изображений (auto-optimize/thumbnail/WebP)
│   │   ├── routes/                 # API маршруты
│   │   │   ├── authRoutes.ts       # POST /api/auth/login
│   │   │   ├── recipeRoutes.ts     # Public API рецептов
│   │   │   ├── adminRoutes.ts      # Protected /api/admin/*
│   │   │   ├── uploadRoutes.ts     # Загрузка изображений
│   │   │   ├── userRoutes.ts       # Управление пользователями
│   │   │   ├── adminLogRoutes.ts   # Логи администраторов
│   │   │   └── index.ts            # Регистрация всех маршрутов
│   │   ├── middleware/             # Middleware
│   │   │   ├── auth.ts             # JWT верификация + role-based access
│   │   │   ├── errorHandler.ts     # Глобальный обработчик ошибок
│   │   │   ├── upload.ts           # Multer конфигурация
│   │   │   ├── rateLimiter.ts      # Rate limiting (IP-based)
│   │   │   ├── validation.ts       # Zod валидация
│   │   │   ├── imageValidation.ts  # Sharp валидация изображений
│   │   │   ├── permissions.ts      # Helpers для проверки прав
│   │   │   └── cache.ts            # Redis caching middleware
│   │   ├── validators/             # Zod схемы
│   │   │   ├── auth.validator.ts   # Login/register
│   │   │   ├── recipe.validator.ts # Рецепты (полная валидация)
│   │   │   ├── comment.validator.ts# Комментарии
│   │   │   └── user.validator.ts   # Пользователи
│   │   ├── utils/                  # Утилиты
│   │   │   ├── jwt.ts              # Генерация/проверка токенов
│   │   │   ├── password.ts         # Bcrypt хеширование
│   │   │   ├── imageProcessor.ts   # Sharp: WebP conversion (convertToWebP, createWebPThumbnail)
│   │   │   ├── spamFilter.ts       # Антиспам логика (6 фильтров)
│   │   │   ├── adminLogger.ts      # Логирование действий админов (audit trail)
│   │   │   └── cacheInvalidation.ts# Smart cache invalidation helpers
│   │   ├── config/                 # Конфигурация
│   │   │   ├── database.ts         # Prisma client
│   │   │   ├── redis.ts            # Redis client (graceful fallback)
│   │   │   └── logger.ts           # Winston logger configuration
│   │   └── index.ts                # Точка входа (Express app)
│   ├── prisma/
│   │   ├── schema.prisma           # Схема БД (9 таблиц)
│   │   └── seed.ts                 # Seed данные (50+ рецептов)
│   ├── public/uploads/             # Загруженные изображения
│   └── package.json
│
├── package.json                    # Корневой package.json (scripts для монорепо)
├── CLAUDE.md                       # 📚 Документация для AI (подробная)
└── README.md                       # 📖 Этот файл
```

### 🗄️ База данных (14 таблиц)

```prisma
- users                    # Пользователи (SUPER_ADMIN/ADMIN/MODERATOR, active status)
- categories               # Категории (с slug, описанием)
- recipes                  # Рецепты (с JSON полями: ingredients, instructions, nutrition, tips; prepTime для времени подготовки)
- recipe_categories        # Связь many-to-many
- comments                 # Комментарии (APPROVED/PENDING/SPAM, rating 1-5)
- newsletter_subscribers   # Email подписчики (verified, verificationToken, unsubscribeToken)
- site_settings            # Настройки сайта (название, логотип, мета-теги)
- static_pages             # Статические страницы (slug, title, content)
- spam_filter_settings     # Настройки антиспама (toggles, thresholds, keywords)
- admin_logs               # Логи действий администраторов (audit trail с IP, user agent)
- smtp_settings            # Настройки email (encrypted, AES-256)
- email_templates          # Шаблоны писем (Handlebars)
- email_logs               # История отправки email
- (+ служебные таблицы Prisma)
```

**JSON поля в PostgreSQL**:
- `recipes.ingredients` - массив {name, amount, quantity?, unit?, category?} - с опциональными полями для категорий и автоматического масштабирования
- `recipes.instructions` - массив {stepNumber, text, images?: string[]}
- `recipes.nutrition` - {calories, protein, fat, carbs}
- `recipes.tips` - массив строк
- `admin_logs.details` - произвольный объект (old/new значения, affected count, и т.д.)

**Новые поля Recipe**:
- `prepTime` (Int?) - время подготовки в минутах (опциональное)
- `cookingTime` (Int) - время приготовления в минутах (обязательное)

---

## 🛠️ Технологический стек

### Frontend
| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19 | UI библиотека (latest) |
| **TypeScript** | 5.0+ | Type safety |
| **Vite** | Latest | Сборщик (быстрый HMR) |
| **React Router** | 7 | Маршрутизация (SSR-ready) |
| **CSS** | Plain CSS | Стилизация (без препроцессоров) |
| **Google Fonts** | Montserrat | Типографика логотипа |
| **DOMPurify** | Latest | XSS защита |
| **react-qr-code** | Latest | Генерация QR кодов |

### Backend
| Технология | Версия | Назначение |
|------------|--------|------------|
| **Node.js** | 18+ | Runtime |
| **Express** | 5 | Web framework (latest) |
| **TypeScript** | 5.0+ | Type safety |
| **Prisma** | Latest | ORM (type-safe) |
| **PostgreSQL** | 12+ | База данных |
| **Redis** | ioredis | Кеширование (optional, graceful fallback) |
| **JWT** | jsonwebtoken | Аутентификация |
| **Bcrypt** | bcrypt | Хеширование паролей (10 rounds) |
| **Multer** | Latest | Загрузка файлов (max 5MB) |
| **Sharp** | Latest | Обработка изображений + валидация |
| **Helmet** | Latest | Security headers |
| **CORS** | Latest | Cross-origin requests |
| **Express Rate Limit** | Latest | Rate limiting (IP-based) |
| **Zod** | Latest | Валидация схем |
| **Winston** | Latest | Логирование (production-ready) |

### DevOps & Tools
- **Monorepo** - npm workspaces
- **Docker** - контейнеризация (Dockerfile + docker-compose)
- **Git** - версионный контроль
- **Prisma Studio** - GUI для БД
- **Environment variables** - конфигурация
- **Winston** - структурированное логирование (rotating files)

---

## 🌐 API Endpoints

### 🔓 Public API

#### Рецепты
```
GET    /api/recipes                # Список (supports ?sort=newest|popular|photo)
GET    /api/recipes/:id            # Детали (только PUBLISHED)
POST   /api/recipes/:id/view       # Инкремент просмотров (throttled 24h)
GET    /api/recipes/search         # Поиск (?q=keyword&page=1&limit=12)
GET    /api/recipes/stats          # Статистика (всего/пользователей/комментариев)
```

#### Категории
```
GET    /api/categories             # Список категорий
GET    /api/categories/:slug/recipes # Рецепты категории
```

#### Комментарии
```
GET    /api/comments/recipe/:id    # Список для рецепта (только APPROVED)
                                   # Query params: page (default 1), limit (default 20, max 100)
                                   # Response: {data: Comment[], pagination: {page, limit, total, totalPages, hasMore}}
POST   /api/comments               # Создать (auto spam check → PENDING/SPAM)
```

#### Другое
```
GET    /api/settings               # Настройки сайта (название, логотип, мета)
GET    /api/static-pages/:slug     # Статическая страница (about/contact/rules/advertising)
POST   /api/newsletter/subscribe   # Подписка на рассылку
GET    /api/health                 # Health check
```

---

### 🔒 Protected API (требует Authorization: Bearer <token>)

#### Аутентификация
```
POST   /api/auth/login             # Вход (username + password → JWT token)
```

#### Админ - Dashboard
```
GET    /api/admin/stats            # Статистика (MOD+)
```

#### Админ - Рецепты
```
GET    /api/admin/recipes          # Все рецепты (включая DRAFT) (MOD+)
GET    /api/admin/recipes/:id      # Детали (MOD+)
POST   /api/admin/recipes          # Создать (MOD+)
PUT    /api/admin/recipes/:id      # Обновить (MOD+)
DELETE /api/admin/recipes/:id      # Удалить (MOD+)
```

#### Админ - Категории
```
GET    /api/admin/categories       # Список (MOD+)
POST   /api/admin/categories       # Создать (MOD+)
PUT    /api/admin/categories/:id   # Обновить (MOD+)
DELETE /api/admin/categories/:id   # Удалить (MOD+)
```

#### Админ - Теги
```
GET    /api/admin/tags             # Список с статистикой использования (MOD+)
PUT    /api/admin/tags/:oldName    # Переименовать глобально (MOD+)
DELETE /api/admin/tags/:name       # Удалить из всех рецептов (MOD+)
```

#### Админ - Комментарии
```
GET    /api/admin/comments         # Все комментарии (любой статус) (MOD+)
PATCH  /api/admin/comments/:id/status  # Изменить статус (MOD+)
DELETE /api/admin/comments/:id    # Удалить (MOD+)
POST   /api/admin/comments/bulk-approve    # Массовое одобрение (MOD+)
POST   /api/admin/comments/bulk-spam       # Массовая пометка спама (MOD+)
POST   /api/admin/comments/bulk-pending    # Массовый возврат в ожидание (MOD+)
DELETE /api/admin/comments/bulk-delete     # Массовое удаление (MOD+)
```

#### Админ - Newsletter
```
GET    /api/admin/newsletter/subscribers  # Список подписчиков (ADMIN+)
```

#### Админ - Пользователи
```
GET    /api/admin/users            # Список (optional ?role=SUPER_ADMIN|ADMIN|MODERATOR) (ADMIN+)
GET    /api/admin/users/:id        # Детали пользователя (ADMIN+)
POST   /api/admin/users            # Создать (role validation: ADMIN can only create MODERATOR) (ADMIN+)
PUT    /api/admin/users/:id        # Обновить (prevents self-privilege escalation) (ADMIN+)
DELETE /api/admin/users/:id        # Удалить (prevents self-deletion, protects last SUPER_ADMIN) (ADMIN+)
PATCH  /api/admin/users/:id/password      # Изменить пароль (ADMIN+)
PATCH  /api/admin/users/:id/status        # Toggle active/inactive (ADMIN+)
```

#### Админ - Статические страницы
```
GET    /api/admin/static-pages     # Список (ADMIN+)
GET    /api/admin/static-pages/:slug # Детали (ADMIN+)
PUT    /api/admin/static-pages/:slug # Обновить (HTML sanitized) (ADMIN+)
```

#### Админ - Настройки сайта
```
GET    /api/admin/settings         # Получить настройки (ADMIN+)
PUT    /api/admin/settings         # Обновить (ADMIN+)
```

#### Админ - Спам-фильтр (только SUPER_ADMIN)
```
GET    /api/admin/spam-filter      # Получить настройки (SUPER_ADMIN)
PUT    /api/admin/spam-filter      # Обновить toggles/thresholds (SUPER_ADMIN)
POST   /api/admin/spam-filter/keywords      # Добавить ключевое слово (SUPER_ADMIN)
DELETE /api/admin/spam-filter/keywords/:keyword # Удалить ключевое слово (SUPER_ADMIN)
```

#### Админ - Логи (только SUPER_ADMIN)
```
GET    /api/admin/logs             # Список логов с фильтрацией (SUPER_ADMIN)
                                   # Query params: page, limit (1-100), userId, action, resource, startDate, endDate
GET    /api/admin/logs/stats       # Статистика логов (total, recent, breakdowns) (SUPER_ADMIN)
```

#### Загрузка файлов (с автоматической конвертацией в WebP)
```
POST   /api/upload/recipe-image    # Загрузить главное фото (MOD+)
                                   # Input: JPEG/PNG/WebP (max 5MB)
                                   # Response: {url: string, thumbnail: string}
                                   # Автоматически конвертирует в WebP (main 1200px + thumbnail 300px)
POST   /api/upload/step-images     # Загрузить фото шагов (max 5) (MOD+)
                                   # Response: {urls: string[], images: [{url, thumbnail}, ...]}
                                   # urls[] - для обратной совместимости (legacy)
```

---

### 📊 Форматы ответов

```typescript
// List endpoints (с пагинацией)
{
  data: [...],
  pagination: {
    page: number,
    limit: number,
    total: number,
    totalPages: number
  }
}

// Single resource
{...} // Прямой объект

// Simple collections
[...] // Прямой массив

// Upload
{url: string} // или {urls: string[]}

// Auth
{
  token: string,
  user: {...}
}

// Error
{
  message: string,
  status: number
}
```

**Всегда проверяйте** `Array.isArray()` перед `.map()`!

---

## 🚦 Production Deployment с Docker

### 🔐 Чеклист безопасности

- [ ] **Сгенерировать надёжные секреты**:
  ```bash
  # JWT Secret (64+ символов)
  node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

  # DB Password (сложный пароль)
  openssl rand -base64 32

  # Email Encryption Key (32 символа)
  node -e "console.log(require('crypto').randomBytes(32).toString('hex').substring(0, 32))"
  ```
- [ ] **Обновить .env** - заполнить production значения
- [ ] **ALLOWED_ORIGINS** - указать production домены
- [ ] **Сменить дефолтные пароли админов** - admin/admin123, admin2/admin456, moderator/moderator123
- [ ] **SSL/HTTPS** - настроить reverse proxy (Nginx/Traefik/Caddy)
- [ ] **.env в .gitignore** - проверить что не коммитится в Git
- [ ] **Backup стратегия** - настроить автоматическое резервное копирование БД и uploads

---

### 🐳 Docker Production Build

#### 1️⃣ Подготовка сервера

```bash
# Обновить систему (Ubuntu/Debian)
sudo apt update && sudo apt upgrade -y

# Установить Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установить Docker Compose (если нет)
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

#### 2️⃣ Настройка production переменных

Создать `.env` файл в корне проекта:
```env
# Database
DB_PASSWORD=<STRONG_PASSWORD_HERE>

# JWT (ОБЯЗАТЕЛЬНО новый секрет для production!)
JWT_SECRET=<GENERATED_64_CHAR_SECRET>

# CORS - Ваши production домены
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Email
EMAIL_ENCRYPTION_KEY=<32_CHAR_KEY>
FRONTEND_URL=https://yourdomain.com
BACKEND_URL=https://yourdomain.com
```

**Важные настройки для docker-compose.yml:**
- Убедитесь что `NODE_ENV=production`
- Проверьте `ALLOWED_ORIGINS` с вашим доменом
- Установите сложные пароли для БД

#### 3️⃣ Запуск production окружения

```bash
# Клонировать репозиторий на сервер
git clone https://github.com/yourusername/soroka-food.git
cd soroka-food

# Создать и настроить .env
nano .env

# Собрать и запустить контейнеры в production режиме
docker-compose up -d --build

# Применить миграции БД
docker-compose exec backend npx prisma migrate deploy

# Загрузить начальные данные (первый запуск)
docker-compose exec backend node dist/prisma/seed.js

# Проверить статус всех сервисов
docker-compose ps

# Проверить логи
docker-compose logs -f
```

#### 4️⃣ Health Check

```bash
# Проверить API
curl http://localhost:3000/api/health
# Должен вернуть: {"status":"ok"}

# Проверить frontend (если настроен reverse proxy)
curl http://localhost
```

---

### 🌐 Настройка Reverse Proxy (Nginx + SSL)

#### Установка Certbot для Let's Encrypt SSL

```bash
sudo apt install nginx certbot python3-certbot-nginx -y
```

#### Конфигурация Nginx

Создать файл `/etc/nginx/sites-available/soroka-food`:

```nginx
# Redirect HTTP to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name yourdomain.com www.yourdomain.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS Server
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;

    # SSL certificates (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;

    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Proxy настройки для frontend
    location / {
        proxy_pass http://localhost:80;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API proxy (если нужен отдельный поддомен api.yourdomain.com)
    # location /api {
    #     proxy_pass http://localhost:3000;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Client max body size (для загрузки изображений)
    client_max_body_size 10M;
}
```

#### Активация конфигурации

```bash
# Создать symlink
sudo ln -s /etc/nginx/sites-available/soroka-food /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Получить SSL сертификат (Let's Encrypt)
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# Перезапустить Nginx
sudo systemctl restart nginx

# Включить автозапуск
sudo systemctl enable nginx
```

---

### 🔄 CI/CD и обновления

#### Обновление приложения

```bash
# 1. Остановить контейнеры
docker-compose down

# 2. Получить новый код
git pull origin main

# 3. Пересобрать и запустить
docker-compose up -d --build

# 4. Применить новые миграции (если есть)
docker-compose exec backend npx prisma migrate deploy

# 5. Проверить статус
docker-compose ps
docker-compose logs -f
```

#### Автоматизация с GitHub Actions (пример)

<details>
<summary>Пример .github/workflows/deploy.yml</summary>

```yaml
name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /path/to/soroka-food
            git pull origin main
            docker-compose down
            docker-compose up -d --build
            docker-compose exec -T backend npx prisma migrate deploy
```
</details>

---

### 💾 Backup и мониторинг

#### Автоматический backup PostgreSQL

Создать скрипт `/root/backup-soroka.sh`:
```bash
#!/bin/bash
BACKUP_DIR="/backups/soroka-food"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# Backup PostgreSQL
docker exec soroka-food-db pg_dump -U postgres soroka-food | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# Backup uploads
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz soroka-food-backend/public/uploads/

# Удалить старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "*.gz" -mtime +7 -delete

echo "Backup completed: $DATE"
```

Добавить в crontab:
```bash
sudo crontab -e
# Ежедневный backup в 3:00
0 3 * * * /root/backup-soroka.sh >> /var/log/soroka-backup.log 2>&1
```

#### Мониторинг

```bash
# Просмотр использования ресурсов контейнерами
docker stats

# Просмотр логов в реальном времени
docker-compose logs -f --tail=100

# Настроить логирование в файл (уже настроено в docker-compose.yml)
# Логи доступны в volume: logs:/app/logs
```

---

### 💻 Рекомендуемые характеристики сервера

#### Минимальные требования
- **CPU**: 2 ядра
- **RAM**: 2GB
- **Диск**: 20GB SSD
- **ОС**: Ubuntu 20.04+ / Debian 10+ / CentOS 8+

#### Рекомендуемые для production
- **CPU**: 4 ядра
- **RAM**: 4GB
- **Диск**: 50GB SSD
- **ОС**: Ubuntu 22.04 LTS

#### Оптимальные (для высоконагруженных проектов)
- **CPU**: 8 ядер
- **RAM**: 8GB+
- **Диск**: 100GB+ SSD
- **ОС**: Ubuntu 22.04 LTS

**Примечание**: Характеристики зависят от количества пользователей, объёма контента и использования Redis кеша.

---

## 🎨 Кастомизация

### 🎨 Стилизация
- **Plain CSS** - все стили в `soroka-food-app/src/styles/`
- **Component-scoped** - Header.css, Home.css, RecipeCard.css и т.д.
- **Google Fonts** - Montserrat для логотипа (можно заменить в index.html)
- **Адаптивность** - breakpoints уже настроены

### 🖼️ Изображения
- **Хелпер** - всегда используйте `getImageUrl(path)` из `utils/image.ts`
- **Backend** - возвращает относительные пути (`/uploads/file.jpg`)
- **Frontend** - конвертирует в полные URL (http://localhost:3000/uploads/file.jpg)
- **Форматы** - JPEG, PNG, WebP
- **Ограничения** - max 5MB, max 5000×5000px

### 🔧 Расширение функционала

#### Добавление новой фичи
1. **Backend**:
   ```typescript
   // src/controllers/myController.ts
   export const myMethod = async (req, res) => {...}

   // src/routes/myRoutes.ts
   router.get('/my-route', myMethod)

   // src/routes/index.ts
   import myRoutes from './myRoutes'
   app.use('/api/my', myRoutes)
   ```

2. **Frontend**:
   ```typescript
   // src/services/api.ts
   myFeature: {
     getData: () => api.get<MyType>('/my/data')
   }

   // src/pages/MyPage.tsx
   const MyPage = () => {...}

   // src/App.tsx
   <Route path="/my" element={<MyPage />} />
   ```

3. **Types** (если нужно):
   ```typescript
   // soroka-food-app/src/types/index.ts
   export interface MyType {...}
   ```

#### Изменение схемы БД
```bash
# 1. Редактировать prisma/schema.prisma
# 2. Создать миграцию
npm run prisma:migrate

# 3. Обновить типы
npm run prisma:generate

# 4. Обновить src/types/index.ts в frontend
```

---

## 🚀 Потенциал для развития

### 📱 Мобильные приложения
- **REST API готов** - полностью type-safe API для iOS/Android
- **React Native** - переиспользовать TypeScript типы и бизнес-логику
- **Shared типы** - вынести в отдельный npm пакет (@soroka-food/types)
- **Offline-first** - синхронизация данных с сервером

### 🌍 Интернационализация
- **i18n** - поддержка нескольких языков (en, ru, uk, de, es)
- **Контент** - хранить переводы в БД или JSON файлах
- **react-i18next** - готовая библиотека для React
- **Автоопределение языка** - по IP-адресу или настройкам браузера

### 📈 SEO & Производительность
- **SSR/SSG** - серверный рендеринг с React Router 7
- **Lazy loading** - code splitting для улучшения первой загрузки
- **Sitemap.xml** - автогенерация для поисковых систем
- **Schema.org** - структурированная разметка рецептов
- **Open Graph** - превью для социальных сетей

### 💰 Монетизация
- **Платные рецепты** - премиум контент с подпиской
- **Партнёрские программы** - ссылки на продукты с кэшбеком
- **Баннерная реклама** - интеграция Google AdSense
- **Спонсорский контент** - размещение рецептов от брендов

### 🎨 UX улучшения
- **Избранное** - сохранение рецептов в личную коллекцию
- **Списки покупок** - автогенерация из ингредиентов с возможностью редактирования
- **Планирование меню** - календарь питания на неделю/месяц
- **PWA** - установка как приложение на рабочий стол
- **Push уведомления** - уведомления о новых рецептах
- **Голосовой поиск** - поиск рецептов голосом
- **Таймеры готовки** - встроенные таймеры для шагов приготовления

### 🤝 Социальные функции
- **Профили пользователей** - регистрация, личные коллекции, история просмотров
- **Лайки/дизлайки** - расширенные реакции на рецепты
- **Ответы на комментарии** - вложенные комментарии (threading)
- **Загрузка фото пользователями** - "Мой вариант рецепта"
- **Рейтинги пользователей** - геймификация и достижения
- **Подписки на авторов** - уведомления о новых рецептах от любимых авторов

### 📊 Аналитика
- **Google Analytics 4** - отслеживание посетителей и поведения
- **Поисковые запросы** - анализ популярных тем для контента
- **Heatmaps** - Hotjar/Yandex Metrika для UX анализа
- **A/B тестирование** - оптимизация конверсии
- **Дашборд аналитики** - встроенная панель статистики для админов

### 🔧 DevOps & Infrastructure
- **CI/CD** - автоматический deploy через GitHub Actions
- **CDN** - Cloudflare/AWS CloudFront для статики и изображений
- **Масштабирование** - horizontal scaling с load balancer
- **Мониторинг** - Sentry (errors), Prometheus (metrics), Grafana (dashboards)
- **Log aggregation** - ELK stack или Loki для централизованных логов

---

## 📚 Документация

### Основная
- **CLAUDE.md** - подробная документация для AI-ассистентов и разработчиков
- **README.md** - этот файл (общая информация)

### Дополнительная
- **Frontend**: `soroka-food-app/README.md` (если есть)
- **Backend**: `soroka-food-backend/README.md` (если есть)
- **Backend Setup**: `soroka-food-backend/SETUP.md` (если есть)

### Полезные ссылки
- [React 19 Docs](https://react.dev/)
- [Express 5 Guide](https://expressjs.com/en/5x/api.html)
- [Prisma Docs](https://www.prisma.io/docs/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [PostgreSQL Manual](https://www.postgresql.org/docs/)

---

## 🐛 Troubleshooting

### Docker

#### Контейнеры не запускаются
```bash
# Проверить статус
docker-compose ps

# Проверить логи
docker-compose logs

# Перезапустить все сервисы
docker-compose down
docker-compose up -d

# Полная переустановка (УДАЛИТ все данные!)
docker-compose down -v
docker-compose up -d --build
```

#### PostgreSQL в контейнере недоступен
```bash
# Проверить health check
docker-compose ps postgres

# Проверить логи БД
docker-compose logs postgres

# Войти в контейнер и проверить
docker-compose exec postgres psql -U postgres -d soroka-food

# Перезапустить PostgreSQL
docker-compose restart postgres
```

#### Ошибка миграций Prisma в Docker
```bash
# 1. Проверить что PostgreSQL запущен
docker-compose ps postgres

# 2. Закрыть Prisma Studio если открыт
docker-compose exec backend pkill -f "prisma studio"

# 3. Применить миграции снова
docker-compose exec backend npx prisma migrate deploy

# 4. Если не помогает - пересоздать БД (ПОТЕРЯ ДАННЫХ!)
docker-compose down -v
docker-compose up -d
docker-compose exec backend npx prisma migrate deploy
docker-compose exec backend node dist/prisma/seed.js
```

#### Порт занят
```bash
# Проверить какой процесс использует порт
sudo lsof -i :80   # Frontend
sudo lsof -i :3000 # Backend
sudo lsof -i :5432 # PostgreSQL

# Остановить конфликтующий контейнер
docker ps
docker stop <container_id>

# Изменить порты в docker-compose.yml
# Например, для frontend:
# ports:
#   - "8080:80"  # вместо "80:80"
```

#### Изображения не отображаются
```bash
# Проверить volume с uploads
docker volume ls
docker volume inspect soroka-food_uploads

# Проверить что файлы есть в контейнере
docker-compose exec backend ls -la public/uploads/

# Проверить права доступа
docker-compose exec backend chmod -R 755 public/uploads/
```

#### Логи контейнера показывают ошибку "Cannot connect to database"
```bash
# Проверить что PostgreSQL готов
docker-compose ps postgres
# Должно быть "healthy" в статусе

# Проверить переменные окружения
docker-compose exec backend env | grep DATABASE_URL

# Дождаться полной загрузки PostgreSQL (health check)
docker-compose up -d
# Подождать 10-15 секунд
docker-compose restart backend
```

#### Ошибка "no space left on device"
```bash
# Очистить неиспользуемые Docker ресурсы
docker system prune -a --volumes

# Проверить использование диска
df -h
docker system df

# Удалить старые образы
docker image prune -a
```

---

### Проблемы локальной установки

<details>
<summary>Развернуть решения проблем</summary>

#### PostgreSQL не запускается
```bash
# Windows
net start postgresql-x64-17

# Linux/Mac
sudo service postgresql start
# или
brew services start postgresql@14
```

#### Ошибка миграций Prisma
```bash
# 1. Закрыть все подключения к БД (включая Prisma Studio)
# 2. Перезапустить PostgreSQL
net restart postgresql-x64-17  # Windows
sudo service postgresql restart # Linux

# 3. Попробовать снова
npm run prisma:migrate
```

#### Порт занят
**Backend (3000)**:
```bash
# Изменить PORT в soroka-food-backend/.env
PORT=3001
```

**Frontend (5173)**:
```
Vite автоматически выберет следующий доступный порт (5174, 5175...)
```

#### CORS ошибки
```env
# soroka-food-backend/.env
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5174
```
</details>

---

## 📄 Лицензия

**MIT License**

Copyright (c) 2025 Soroka Food

Разрешается бесплатное использование, копирование, модификация и распространение.

---

## 📧 Контакты и поддержка

- **Issues**: [GitHub Issues](https://github.com/ShalunBdk/soroka-food/issues)
- **Discussions**: [GitHub Discussions](https://github.com/ShalunBdk/soroka-food/discussions)
- **Email**: your@email.com (если нужно)

---

<div align="center">

**Soroka Food** - Ваш идеальный кулинарный блог 🍳

[⬆ Наверх](#-soroka-food---профессиональный-кулинарный-блог)

</div>
